# Square Integration Environment Variables
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control!

# ============================================
# SERVER-SIDE ONLY (NEVER use NEXT_PUBLIC_)
# ============================================
# These variables are kept secret and only accessible on the server

# Square Access Token - Get from https://developer.squareup.com/apps
# Use production token for live payments
SQUARE_ACCESS_TOKEN=your_access_token_here

# Optional: Separate sandbox token for testing
SQUARE_SANDBOX_ACCESS_TOKEN=your_sandbox_token_here

# Square Environment - "sandbox" for testing, "production" for live
SQUARE_ENVIRONMENT=sandbox

# Square Location ID - Your business location ID from Square Dashboard
# This is the server-side copy (keep separate from NEXT_PUBLIC_ version)
SQUARE_LOCATION_ID=your_location_id_here

# ============================================
# CLIENT-SIDE (Safe to expose in browser)
# ============================================
# These variables are needed by Square Web Payments SDK in the browser

# Square Application ID - Public identifier for your Square app
NEXT_PUBLIC_SQUARE_APPLICATION_ID=your_app_id_here

# Square Location ID - Same value as SQUARE_LOCATION_ID above
# Needed for browser-side Square SDK initialization
NEXT_PUBLIC_SQUARE_LOCATION_ID=your_location_id_here

# ============================================
# SECURITY NOTES
# ============================================
# ✅ DO use NEXT_PUBLIC_ for: Application ID, Location ID (public identifiers)
# ❌ NEVER use NEXT_PUBLIC_ for: Access tokens, sandbox tokens, or any secrets
# ⚠️ Anything with NEXT_PUBLIC_ prefix is bundled into browser JavaScript
# ⚠️ Always keep .env.local in .gitignore
